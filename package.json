{
  "private": true,
  "scripts": {
    "test": "npm run lint && mocha ./node_modules/html-tests/task-stub/test",
    "lint": "stylelint *.css **/*.css && html-validator --file index.html --verbose && htmllint *.html"
  },
  "dependencies": {
    "html-tests": "2.0.0",
    "html-validator-cli": "4.1.1",
    "htmllint-cli": "0.0.7",
    "mocha": "5.2.0",
    "should": "13.2.3",
    "stylelint": "9.6.0",
    "stylelint-config-hrundel": "2.0.0",
    "test": "0.6.0"
  }
}
